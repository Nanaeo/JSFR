//变量控制 须知防止越权读取分应用隔离变量 同时公开部分
function initVarControl(){}
function setAuth(token,address){}
function getAuth(token,address){}
function readVar(token,address){}
function writeVar(token,address){}
//需要实现的
